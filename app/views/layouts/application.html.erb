<!DOCTYPE html>
<html lang="en">
  <%= render "layouts/head" %>

  <body class="<%= body_class %>">
    <%= render "navigation" %>

    <% if @current_tab %>
      <%= render "tabs" %>
    <% end %>

    <section class="container tab-content" role="main">
      <%= render "flashes" %>
      <%= yield %>
    </section>
  </body>
</html>
