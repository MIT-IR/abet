<table>
  <tr>
    <td>
      <%= t(".no_assessments", assessment_type: assessment_type) %>

      <% if @filtered_outcome.present? %>
        <%= t(".for_outcome", name: @filtered_outcome.name.capitalize) %>
      <% end %>
    </td>
  </tr>
</table>
