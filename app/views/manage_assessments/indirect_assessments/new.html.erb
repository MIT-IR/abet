<% tab(TabHelper::ASSESSMENTS) %>

<h1><%= t(".heading.#{params[:type].downcase}") %></h1>
<%= outcome_details(@outcome || @assessment.outcomes.first) %>

<%= simple_form_for @assessment, url: manage_assessments_outcome_indirect_assessments_path(@outcome) do |f| %>
  <%= render "#{@assessment.to_partial_path}_form", f: f %>
<% end %>
